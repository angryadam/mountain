<nav class="navbar has-shadow" role="navigation">
	<div class="container">
		<div class="navbar-brand">
			<a class="navbar-item" href="">
				<i class="fas fa-mountain"></i>
			</a>
			<a role="button" class="navbar-burger burger">
				<span aria-hidden="true"></span>
				<span aria-hidden="true"></span>
				<span aria-hidden="true"></span>
			</a>
		</div>

		<div class="navbar-menu">
			<div class="navbar-start">
			<span class="navbar-item">
				<%= link_to 'Your Loans', root_path, class: 'button is-text' %>
			</span>
				<span class="navbar-item">
				<%= link_to(new_provider_path, class: 'button is-primary is-outlined') do %>
					<i class="fas fa-plus"></i>
				<% end %>
			</span>
			</div>
			<div class="navbar-end">
				<div class="navbar-item">
					<div class="buttons">
						<% if signed_in? %>
							<%= button_to 'Sign Out', sign_out_path, method: :delete, class: 'button is-light' %>
						<% else %>
							<%= link_to 'Sign Up', sign_up_path, class: 'button is-primary' %>
							<%= link_to 'Login', sign_in_path, class: 'button is-light' %>
						<% end %>
					</div>
				</div>
			</div>
		</div>
	</div>
</nav>
